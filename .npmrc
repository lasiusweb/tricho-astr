# Secure defaults for installs in CI and developer machines
# Disable lifecycle scripts to avoid arbitrary postinstall execution
ignore-scripts=true
# Block git-based dependencies
allow-git=none
# Minimum release age (days) for packages (tooling read-only hint)
min-release-age=30
